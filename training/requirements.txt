# DocSmile CPT training requirements — TRAINING STACK ONLY.
# Other (non-conflicting) utility packages are installed separately by
# setup_vast.sh before this file is processed.
#
# Target Vast.ai image: vastai/pytorch:2.4.1-cuda-12.1.1-py311-22.04
#   (any torch 2.4.x / CUDA 12.1 image works)
#
# CONSERVATIVE pin matrix for torch 2.4.x. Do not loosen any of these
# without testing — the upstream packages are tightly coupled and pip
# will happily pick incompatible combinations.
#
# Why each pin:
#   - torch 2.4.x: provided by the Vast.ai image, do NOT reinstall.
#   - transformers 4.46.3: last 4.46.x patch. transformers 4.51+ uses
#     torch.int1 which only exists in torch >= 2.6.
#   - tokenizers 0.20.3: matches transformers 4.46.x lockstep.
#   - trl 0.12.2: predates the trl/transformers API break in trl 0.15+.
#   - peft 0.13.2: compatible with this transformers range.
#   - accelerate 1.1.1: compatible with transformers 4.46.x.
#   - bitsandbytes 0.44.1: stable NF4 quantization on CUDA 12.1.
#   - unsloth 2025.6.1 + unsloth_zoo 2025.6.4: last release pair that works
#     on torch 2.4.x. Later versions hit `torch._inductor.config` import
#     errors on torch < 2.5.
#
# MUST install with --no-deps so pip doesn't pull newer transformers etc:
#   pip install --force-reinstall --no-deps -r requirements.txt

unsloth==2025.6.1
unsloth-zoo==2025.6.4
transformers==4.46.3
tokenizers==0.20.3
trl==0.12.2
peft==0.13.2
accelerate==1.1.1
bitsandbytes==0.44.1
datasets==3.1.0
